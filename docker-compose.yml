web:
  build: .
  command: lein run
  ports:
    - "8081:8081"
#    - "6379:6379" //bind: address already in use
  volumes:
    - ".:/mnt"
  # restart: always

redis:
#  restart: always
  ports:
  - 6379:6379  #端口号
  image: redis #docker 镜像
  command: redis-server